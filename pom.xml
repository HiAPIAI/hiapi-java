<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>ai.hiapi</groupId>
  <artifactId>hiapi</artifactId>
  <version>0.2.1</version>
  <packaging>jar</packaging>

  <name>HiAPI Java SDK</name>
  <description>Zero-dependency Java client for the HiAPI unified async task API (/v1/tasks).</description>
  <url>https://hiapi.ai</url>

  <licenses>
    <license>
      <name>MIT License</name>
      <url>https://opensource.org/licenses/MIT</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <!-- Maven Central requires at least one developer entry. Confirm the email
       below points at a real inbox before publishing, or replace it. -->
  <developers>
    <developer>
      <id>hiapi</id>
      <name>HiAPI</name>
      <email>support@hiapi.ai</email>
      <organization>HiAPI</organization>
      <organizationUrl>https://hiapi.ai</organizationUrl>
    </developer>
  </developers>

  <scm>
    <connection>scm:git:https://github.com/HiAPIAI/hiapi-java.git</connection>
    <developerConnection>scm:git:ssh://git@github.com/HiAPIAI/hiapi-java.git</developerConnection>
    <url>https://github.com/HiAPIAI/hiapi-java</url>
    <tag>HEAD</tag>
  </scm>

  <properties>
    <maven.compiler.release>11</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <junit.version>5.10.2</junit.version>
  </properties>

  <dependencies>
    <!-- ZERO runtime dependencies. The entries below are test-scope only. -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!-- Activate with `mvn -Prelease ...` to build the source + javadoc jars,
         GPG-sign every artifact, and publish to the Sonatype Central Portal.
         Kept out of the default build so plain `mvn package` needs no GPG key. -->
    <profile>
      <id>release</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-source-plugin</artifactId>
            <version>3.3.1</version>
            <executions>
              <execution>
                <id>attach-sources</id>
                <goals>
                  <goal>jar-no-fork</goal>
                </goals>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-javadoc-plugin</artifactId>
            <version>3.6.3</version>
            <executions>
              <execution>
                <id>attach-javadocs</id>
                <goals>
                  <goal>jar</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <!-- Ship even if a few elements lack doc comments. -->
              <doclint>none</doclint>
              <quiet>true</quiet>
              <source>11</source>
            </configuration>
          </plugin>

          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <version>3.2.4</version>
            <executions>
              <execution>
                <id>sign-artifacts</id>
                <phase>verify</phase>
                <goals>
                  <goal>sign</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <!-- Loopback lets GPG read the passphrase non-interactively,
                   which macOS / CI need. -->
              <gpgArguments>
                <arg>--pinentry-mode</arg>
                <arg>loopback</arg>
              </gpgArguments>
            </configuration>
          </plugin>

          <plugin>
            <groupId>org.sonatype.central</groupId>
            <artifactId>central-publishing-maven-plugin</artifactId>
            <version>0.11.0</version>
            <extensions>true</extensions>
            <configuration>
              <!-- Matches the <server><id> in ~/.m2/settings.xml. -->
              <publishingServerId>central</publishingServerId>
              <!-- true = publish automatically once validation passes (first release
                   was reviewed and published manually; flow is now trusted). -->
              <autoPublish>true</autoPublish>
              <waitUntil>validated</waitUntil>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
